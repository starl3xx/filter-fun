{
  "name": "@filter-fun/cadence",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "filter.fun season cadence — single source of truth for hour-anchors (launch end / hard cut / settlement) read by scheduler + indexer. No transitive deps so it's safe to import from anywhere.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
