{
  "name": "@filter-fun/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "filter.fun smart contract suite (Foundry).",
  "scripts": {
    "build": "forge build",
    "test": "forge test --isolate --threads 1",
    "test:gas": "forge test --isolate --threads 1 --gas-report",
    "coverage": "forge coverage --report summary",
    "fmt": "forge fmt",
    "fmt:check": "forge fmt --check",
    "deploy:sepolia": "./script/deploy-sepolia.sh",
    "deploy:sepolia:seed": "./script/deploy-sepolia.sh --seed-filter"
  }
}
