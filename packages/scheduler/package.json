{
  "name": "@filter-fun/scheduler",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "filter.fun scheduler. Consumes oracle settlement payloads and drives the on-chain settlement loop (submitSettlement → liquidate(loser) → finalize) via viem.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@filter-fun/oracle": "*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
