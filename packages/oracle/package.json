{
  "name": "@filter-fun/oracle",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "filter.fun oracle payload builder. Consumes scoring + holder snapshots, produces SeasonVault.submitSettlement calldata and the rollover Merkle tree.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@filter-fun/scoring": "*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
